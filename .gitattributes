* text=auto eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf

# Historical calibration artifacts are byte-addressed, including line endings.
# Preserve the original bytes without losing readable JSON diffs.
evaluation/parity/*.json -text diff whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
